package objects.value.map;

/**
 * The NotPurchasableData is a class for the network communication that holds all needed information
 */
public class NotPurchasableData extends FieldData {
	/**
	 * @param fieldNumber the value determines the id
	 * @param name        the value determines the name
	 */
	//TODO remove unnecessary parameter
	public NotPurchasableData(int fieldNumber, String name) {
		super(fieldNumber, name);
	}
}
